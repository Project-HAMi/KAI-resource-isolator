name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-rc[0-9]+'

concurrency:
  group: kai-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  docker:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Compute metadata
        id: meta
        run: |
          FULL_TAG="${GITHUB_REF#refs/tags/}"        # v1.0.0
          VERSION="${GITHUB_REF#refs/tags/v}"         # 1.0.0  (without v prefix)

          REPO="docker.io/projecthami/kai-resource-isolator"
          echo "tags=${REPO}:${FULL_TAG},${REPO}:latest" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "full_tag=${FULL_TAG}" >> "$GITHUB_OUTPUT"

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_TOKEN }}
          password: ${{ secrets.DOCKERHUB_PASSWD }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}

    outputs:
      version: ${{ steps.meta.outputs.version }}
      full_tag: ${{ steps.meta.outputs.full_tag }}

  chart:
    needs: docker
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Package and push chart
        run: |
          VERSION="${{ needs.docker.outputs.version }}"
          FULL_TAG="${{ needs.docker.outputs.full_tag }}"
          CHART_VERSION="${VERSION}-chart"
          CHART_DIR="chart/kai-resource-isolator"

          sed -i "s/^version:.*/version: ${CHART_VERSION}/" "${CHART_DIR}/Chart.yaml"
          sed -i "s/^appVersion:.*/appVersion: \"${VERSION}\"/" "${CHART_DIR}/Chart.yaml"
          sed -i "s/^  tag:.*/  tag: ${FULL_TAG}/" "${CHART_DIR}/values.yaml"

          helm package "${CHART_DIR}" --version "${CHART_VERSION}"

          echo "${{ secrets.DOCKERHUB_PASSWD }}" | helm registry login docker.io \
            --username "${{ secrets.DOCKERHUB_TOKEN }}" --password-stdin

          helm push "kai-resource-isolator-${CHART_VERSION}.tgz" oci://docker.io/projecthami/kai-resource-isolator
